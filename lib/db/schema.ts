// Drizzle schema — the source of truth for the database (CLAUDE.md).
//
// The full schema (organizations, competitions, teams, matches, sets,
// standings_cache, etc. — see PRD.md Section 9) is built in Phase 1.
// This placeholder exists so the Drizzle client and drizzle-kit config
// resolve during Phase 0 scaffolding.

export {};
